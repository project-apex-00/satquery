# Copy this to .env for local dev. On Railway, set these under Project -> Variables instead.

GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override where the classifier checkpoint is expected on disk
RS_CHECKPOINT_PATH=./models/rs-eurosat-classifier/rs_classifier.pt
